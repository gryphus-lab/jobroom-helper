[project]
name = "jobroom-helper"
version = "0.1.0"
description = "Selenium automation that reads from a local SQLite tracker and fills web forms"
requires-python = ">=3.12"
dependencies = [
    "selenium>=4.20.0",
    "pandas>=2.2.0",
    "httpx>=0.27.0,<1.0.0",
    "httpcore>=1.0.5,<2.0.0",
    "webdriver-manager>=4.0.0",
    "python-dotenv>=1.0.0",
    "beautifulsoup4>=4.12.0",
]

[project.scripts]
autofill = "jobroom_helper.__main__:main"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
exclude = ["**/.venv/**", "**/tests/**", "**/build/**"]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=src/jobroom_helper --cov-report=html --cov-report=term-missing --cov-report=xml:coverage.xml"

[tool.coverage.run]
source = ["src/jobroom_helper"]
omit = ["**/tests/**"]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.15",
]
